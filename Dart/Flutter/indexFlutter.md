# Flutter
2023-06-10
tags: [📱 indexDart](📱%20indexDart.md)

## Installation

good luck :)

* user full info:

```bash
export PATH=$PATH:/home/kkphoenix/Downloads/android-studio/bin
export PATH="$HOME/flutter/bin:$PATH"
export PATH="$HOME/Android/Sdk/flutter/bin:$PATH"
export CHROME_EXECUTABLE=/usr/bin/google-chrome-stable
```


  * Flutter sdk and cmd-tools can be downloaded in android studio
  * main file and "src" is lib folder

## Running 


* `flutter devices` can check devices connected
* `flutter pub get` install dependencies
* `flutter run` can run in device if there is a device with USB debugging available in usb.

## Creating a Project

You can run `flutter create <projectName>` to create a flutter project




